# Lumi Vagalume 🪲✨ (RAG offline para crianças)

Projeto educativo (7–11 anos) com visual infantil e RAG (biblioteca em Markdown).

## ✅ Roda de dois jeitos

### 1) Sem instalar nada extra (padrão)

Funciona com RAG offline (busca lexical TF-IDF) + resposta infantil guiada por template.

### 2) Com Ollama (opcional, offline “IA de verdade”)

Se tiver Ollama + modelo, o chat usa LLM local automaticamente.

## 🚀 Como rodar

```bash
npm install
npm run ingest
npm run dev
```
